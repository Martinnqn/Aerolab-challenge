import React, { useState } from "react";
import styled from "styled-components/macro";

const Product = ({}) => {
  return <ContainerProduct></ContainerProduct>;
};

const ContainerProduct = styled.div`
  display: grid;
`;
